shader_type canvas_item;

#ifdef GL_ES
precision mediump float;
#endif

// uniform float u_time;
vec4 red(){
    return vec4(1.0,0.0,0.0,1.0);
}

void fragment() {
	//COLOR = red();
}

void vertex(){
//	VERTEX = VERTEX + (1.0, 1.5);
}
